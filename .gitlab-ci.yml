        name: Trigger Netlify Build
        on:
          push:
            branches:
              - main # or your desired branch
        jobs:
          build:
            runs-on: ubuntu-latest
            steps:
              - name: Trigger Netlify Build
                run: curl -X POST -d '{}' YOUR_NETLIFY_BUILD_HOOK_URL